import 'package:flutter/material.dart';

ThemeData appTheme() {
  return ThemeData(
    primaryColor: Colors.white,
    accentColor: Colors.orange,
    //Add colors according to your needs and theme setup according to your needs.
  );
}
